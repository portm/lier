import { Context } from '../tokenizer'
import autocomplete, {
    types,
    decorates,
    dollar,
    tips,
    up,
} from './autocomplete'
import tokenizer from './tokenizer'

export default {
    tokenizer,
    autocomplete,
    types,
    decorates,
    dollar,
    tips,
    up,
}
